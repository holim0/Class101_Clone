import styeld from "styled-components";

const CommonContainer = styeld.div`
    border-bottom: 2px solid rgba(16, 22, 26, 0.15);
`;

const CommonImg = styeld.img`
    width: 100%;
`;

export { CommonContainer, CommonImg };
